(*
	propargate:
	Leaf n -> leaf n
	Node ((p,n),subtrees)	muovi= aplicacino de propiedad a a t di subtrees
							mouvi= List.map propagate subtre
									x = mas y min tre i valori delle nodici degli alb in muovi (if p = max then maxl else mine)(valoridici nuovi)
							subst = [ t1,...,tn] 
							[propagate t1,...,propagate tn]

*)